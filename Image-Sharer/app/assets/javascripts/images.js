// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
/*$('.form_style').submit(function() {

 var regex = /^(http)s?(:)\/\/.[\S]+[.](png|jpeg|jpg|gif|tiff|bmp)$/;
 if(!regex.test($('.form_style #imgURL').val())) {
   if (!$('.form_style #imgURL').contains("<span style = 'message'>Invalid Image URL!</span>")) {
     $('.form_style #imgURL').append("<span style = 'message'>Invalid Image URL!</span>");
   }
 return false;
 }
 return true;
 });
*/
